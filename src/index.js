import * as constants from './constants';
import decorators from './decorators';
import middleware from './middleware';
import reducer from './reducer';

export default { ...constants, middleware, reducer, decorators };
